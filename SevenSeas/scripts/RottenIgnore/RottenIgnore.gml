/// @description clean up the mess
function RottenIgnore() {

	if irandom(1)
	    with instance_create(0,0,ConditionTimer)
	        condition = RottedPlanks

	script_execute(closeEventCode, 2)
}
